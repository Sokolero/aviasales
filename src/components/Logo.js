import React from "react";

// ========= Component =========================================================
const Logo = (props) => {
  return(
    <img src="images/Logo.png" alt="page-logo-with-planner" className="page-logo"/>
  );
};

export default Logo;
